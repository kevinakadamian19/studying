// Go through entire array
// Check if the right number is smaller than left number
// If no do nothing, if yes then move number to left

// randomArray = [2, 4, 6, 0]

l
function insertionSort(unsortedArray) {


    return unsortedArray
}